// Auth Lambda handler — stub; full implementation added in a later task.
export const handler = async (_event: unknown) => ({
  statusCode: 501,
  body: JSON.stringify({ message: 'Not implemented' }),
})
